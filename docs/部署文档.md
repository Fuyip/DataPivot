# 数枢 (DataPivot) 部署文档

## 1. 部署架构

### 1.1 生产环境架构
```
                    ┌─────────────┐
                    │   用户浏览器  │
                    └──────┬──────┘
                           │ HTTPS
                    ┌──────▼──────┐
                    │    Nginx    │
                    │  (反向代理)  │
                    └──────┬──────┘
                           │
              ┌────────────┴────────────┐
              │                         │
       ┌──────▼──────┐          ┌──────▼──────┐
       │  Vue前端     │          │  FastAPI    │
       │  (静态文件)  │          │  (后端API)  │
       └─────────────┘          └──────┬──────┘
                                       │
                          ┌────────────┼────────────┐
                          │            │            │
                   ┌──────▼──────┐ ┌──▼───┐  ┌────▼────┐
                   │    MySQL    │ │Redis │  │ Celery  │
                   │   (数据库)   │ │(缓存)│  │(任务队列)│
                   └─────────────┘ └──────┘  └─────────┘
```

### 1.2 服务器配置建议

| 环境 | CPU | 内存 | 硬盘 | 说明 |
|------|-----|------|------|------|
| 开发环境 | 2核 | 4GB | 50GB | 本地开发 |
| 测试环境 | 4核 | 8GB | 100GB | 功能测试 |
| 生产环境 | 8核+ | 16GB+ | 500GB+ | 正式运行 |

## 2. 服务器准备

### 2.1 系统要求
- **操作系统**: Ubuntu 20.04 LTS / CentOS 8+ / Debian 11+
- **Python**: 3.9+
- **Node.js**: 16+
- **MySQL**: 8.0+
- **Nginx**: 1.18+

### 2.2 安装基础软件

#### Ubuntu/Debian
```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装Python 3.9+
sudo apt install python3.9 python3.9-venv python3-pip -y

# 安装Node.js
curl -fsSL https://deb.nodesource.com/setup_16.x | sudo -E bash -
sudo apt install nodejs -y

# 安装MySQL
sudo apt install mysql-server -y

# 安装Nginx
sudo apt install nginx -y

# 安装Git
sudo apt install git -y

# 安装其他依赖
sudo apt install build-essential libssl-dev libffi-dev python3-dev -y
```

#### CentOS/RHEL
```bash
# 更新系统
sudo yum update -y

# 安装Python 3.9
sudo yum install python39 python39-devel -y

# 安装Node.js
curl -fsSL https://rpm.nodesource.com/setup_16.x | sudo bash -
sudo yum install nodejs -y

# 安装MySQL
sudo yum install mysql-server -y

# 安装Nginx
sudo yum install nginx -y

# 安装Git
sudo yum install git -y
```

### 2.3 创建部署用户
```bash
# 创建应用用户
sudo useradd -m -s /bin/bash datapivot

# 设置密码
sudo passwd datapivot

# 添加sudo权限（可选）
sudo usermod -aG sudo datapivot

# 切换到应用用户
su - datapivot
```

## 3. 数据库部署

### 3.1 MySQL配置

#### 3.1.1 安全配置
```bash
# 运行安全配置脚本
sudo mysql_secure_installation
```

#### 3.1.2 创建数据库和用户
```bash
# 登录MySQL
sudo mysql -u root -p

# 执行SQL
CREATE DATABASE datapivot CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'datapivot'@'localhost' IDENTIFIED BY 'strong_password_here';
GRANT ALL PRIVILEGES ON datapivot.* TO 'datapivot'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

#### 3.1.3 优化MySQL配置
```bash
# 编辑配置文件
sudo vim /etc/mysql/mysql.conf.d/mysqld.cnf
```

添加以下配置：
```ini
[mysqld]
# 字符集
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci

# 连接数
max_connections=500
max_connect_errors=1000

# InnoDB配置
innodb_buffer_pool_size=2G
innodb_log_file_size=512M
innodb_flush_log_at_trx_commit=2
innodb_flush_method=O_DIRECT

# 查询缓存（MySQL 8.0已移除）
# query_cache_type=0

# 慢查询日志
slow_query_log=1
slow_query_log_file=/var/log/mysql/slow.log
long_query_time=2

# 二进制日志
log_bin=/var/log/mysql/mysql-bin.log
expire_logs_days=7
max_binlog_size=100M
```

重启MySQL：
```bash
sudo systemctl restart mysql
```

### 3.2 Redis安装（可选）
```bash
# Ubuntu/Debian
sudo apt install redis-server -y

# CentOS/RHEL
sudo yum install redis -y

# 启动Redis
sudo systemctl start redis
sudo systemctl enable redis

# 配置Redis密码
sudo vim /etc/redis/redis.conf
# 找到 # requirepass foobared
# 改为 requirepass your_redis_password

# 重启Redis
sudo systemctl restart redis
```

## 4. 后端部署

### 4.1 部署代码

```bash
# 切换到应用用户
su - datapivot

# 创建应用目录
mkdir -p /home/datapivot/app
cd /home/datapivot/app

# 克隆代码
git clone <repository_url> .

# 或上传代码
# scp -r DataPivot datapivot@server:/home/datapivot/app/
```

### 4.2 配置Python环境

```bash
# 创建虚拟环境
python3.9 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 升级pip
pip install --upgrade pip

# 安装依赖
pip install -r requirements.txt

# 安装生产环境依赖
pip install gunicorn uvicorn[standard]
```

### 4.3 配置环境变量

```bash
# 创建生产环境配置
vim .env.prod
```

内容：
```env
DEBUG_MODE=false

MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=datapivot
MYSQL_PASSWORD=strong_password_here
MYSQL_DB=datapivot

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

SECRET_KEY=your-very-long-secret-key-change-this
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

HAIJING_API_KEY=your_api_key
HAIJING_BASE_URL=https://api.example.com

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=/home/datapivot/app/logs/app.log
```

### 4.4 创建Systemd服务

```bash
# 创建服务文件
sudo vim /etc/systemd/system/datapivot.service
```

内容：
```ini
[Unit]
Description=DataPivot FastAPI Application
After=network.target mysql.service

[Service]
Type=notify
User=datapivot
Group=datapivot
WorkingDirectory=/home/datapivot/app
Environment="PATH=/home/datapivot/app/venv/bin"
ExecStart=/home/datapivot/app/venv/bin/gunicorn backend.main:app \
    --workers 4 \
    --worker-class uvicorn.workers.UvicornWorker \
    --bind 127.0.0.1:8000 \
    --timeout 120 \
    --access-logfile /home/datapivot/app/logs/access.log \
    --error-logfile /home/datapivot/app/logs/error.log \
    --log-level info
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启动服务：
```bash
# 重载systemd
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start datapivot

# 设置开机自启
sudo systemctl enable datapivot

# 查看状态
sudo systemctl status datapivot

# 查看日志
sudo journalctl -u datapivot -f
```

### 4.5 配置Celery（可选）

```bash
# 创建Celery服务
sudo vim /etc/systemd/system/datapivot-celery.service
```

内容：
```ini
[Unit]
Description=DataPivot Celery Worker
After=network.target redis.service

[Service]
Type=forking
User=datapivot
Group=datapivot
WorkingDirectory=/home/datapivot/app
Environment="PATH=/home/datapivot/app/venv/bin"
ExecStart=/home/datapivot/app/venv/bin/celery -A backend.celery_app worker \
    --loglevel=info \
    --logfile=/home/datapivot/app/logs/celery.log
Restart=always

[Install]
WantedBy=multi-user.target
```

启动Celery：
```bash
sudo systemctl start datapivot-celery
sudo systemctl enable datapivot-celery
```

## 5. 前端部署

### 5.1 构建前端

```bash
# 进入前端目录
cd /home/datapivot/app/frontend

# 安装依赖
npm install

# 配置生产环境变量
vim .env.production
```

内容：
```env
VITE_API_BASE_URL=https://your-domain.com/api/v1
VITE_APP_TITLE=数枢 DataPivot
```

构建：
```bash
# 构建生产版本
npm run build

# 构建产物在 dist/ 目录
```

### 5.2 配置Nginx

```bash
# 创建Nginx配置
sudo vim /etc/nginx/sites-available/datapivot
```

内容：
```nginx
# HTTP重定向到HTTPS
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}

# HTTPS配置
server {
    listen 443 ssl http2;
    server_name your-domain.com;

    # SSL证书配置
    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # 日志
    access_log /var/log/nginx/datapivot_access.log;
    error_log /var/log/nginx/datapivot_error.log;

    # 前端静态文件
    location / {
        root /home/datapivot/app/frontend/dist;
        try_files $uri $uri/ /index.html;

        # 缓存配置
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # API代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时设置
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;

        # WebSocket支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # 文件上传大小限制
    client_max_body_size 100M;

    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript
               application/x-javascript application/xml+rss
               application/json application/javascript;
}
```

启用配置：
```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/datapivot /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启Nginx
sudo systemctl restart nginx
```

### 5.3 配置SSL证书

#### 使用Let's Encrypt（推荐）
```bash
# 安装Certbot
sudo apt install certbot python3-certbot-nginx -y

# 获取证书
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo certbot renew --dry-run
```

#### 使用自签名证书（仅测试）
```bash
# 创建证书目录
sudo mkdir -p /etc/nginx/ssl

# 生成自签名证书
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
    -keyout /etc/nginx/ssl/key.pem \
    -out /etc/nginx/ssl/cert.pem
```

## 6. 数据迁移

### 6.1 导入初始数据

```bash
# 激活虚拟环境
source /home/datapivot/app/venv/bin/activate

# 运行迁移脚本（如果有）
cd /home/datapivot/app

# 导入示例数据
python scripts/init_db.py
```

### 6.2 从旧系统迁移

```bash
# 备份旧数据库
mysqldump -u root -p old_database > backup.sql

# 导入到新数据库
mysql -u datapivot -p datapivot < backup.sql

# 运行数据清洗脚本
python 银行流水清洗重构-经侦-多线程.py
```

## 7. 监控与日志

### 7.1 配置日志轮转

```bash
# 创建logrotate配置
sudo vim /etc/logrotate.d/datapivot
```

内容：
```
/home/datapivot/app/logs/*.log {
    daily
    rotate 30
    compress
    delaycompress
    notifempty
    create 0640 datapivot datapivot
    sharedscripts
    postrotate
        systemctl reload datapivot > /dev/null 2>&1 || true
    endscript
}
```

### 7.2 系统监控

#### 安装监控工具
```bash
# 安装htop
sudo apt install htop -y

# 安装iotop（磁盘IO监控）
sudo apt install iotop -y

# 安装nethogs（网络监控）
sudo apt install nethogs -y
```

#### 配置Prometheus（可选）
```bash
# 安装Prometheus
wget https://github.com/prometheus/prometheus/releases/download/v2.40.0/prometheus-2.40.0.linux-amd64.tar.gz
tar xvfz prometheus-*.tar.gz
cd prometheus-*

# 配置prometheus.yml
vim prometheus.yml
```

### 7.3 错误追踪

#### 配置Sentry（可选）
```bash
# 安装Sentry SDK
pip install sentry-sdk[fastapi]
```

在代码中配置：
```python
import sentry_sdk
from sentry_sdk.integrations.fastapi import FastApiIntegration

sentry_sdk.init(
    dsn="your-sentry-dsn",
    integrations=[FastApiIntegration()],
    traces_sample_rate=1.0,
)
```

## 8. 备份策略

### 8.1 数据库备份

#### 创建备份脚本
```bash
# 创建备份脚本
vim /home/datapivot/scripts/backup_db.sh
```

内容：
```bash
#!/bin/bash

# 配置
DB_USER="datapivot"
DB_PASS="your_password"
DB_NAME="datapivot"
BACKUP_DIR="/home/datapivot/backups"
DATE=$(date +%Y%m%d_%H%M%S)

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
mysqldump -u $DB_USER -p$DB_PASS $DB_NAME | gzip > $BACKUP_DIR/db_backup_$DATE.sql.gz

# 删除30天前的备份
find $BACKUP_DIR -name "db_backup_*.sql.gz" -mtime +30 -delete

echo "Backup completed: db_backup_$DATE.sql.gz"
```

设置权限和定时任务：
```bash
# 设置执行权限
chmod +x /home/datapivot/scripts/backup_db.sh

# 添加到crontab
crontab -e

# 每天凌晨2点备份
0 2 * * * /home/datapivot/scripts/backup_db.sh >> /home/datapivot/logs/backup.log 2>&1
```

### 8.2 文件备份

```bash
# 备份上传文件和日志
tar -czf /home/datapivot/backups/files_$(date +%Y%m%d).tar.gz \
    /home/datapivot/app/data \
    /home/datapivot/app/bank_statements \
    /home/datapivot/app/logs
```

## 9. 安全加固

### 9.1 防火墙配置

```bash
# 安装UFW
sudo apt install ufw -y

# 默认策略
sudo ufw default deny incoming
sudo ufw default allow outgoing

# 允许SSH
sudo ufw allow 22/tcp

# 允许HTTP/HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# 启用防火墙
sudo ufw enable

# 查看状态
sudo ufw status
```

### 9.2 SSH安全配置

```bash
# 编辑SSH配置
sudo vim /etc/ssh/sshd_config
```

修改：
```
# 禁用root登录
PermitRootLogin no

# 禁用密码登录（使用密钥）
PasswordAuthentication no

# 修改SSH端口（可选）
Port 2222
```

重启SSH：
```bash
sudo systemctl restart sshd
```

### 9.3 Fail2ban配置

```bash
# 安装Fail2ban
sudo apt install fail2ban -y

# 配置
sudo vim /etc/fail2ban/jail.local
```

内容：
```ini
[DEFAULT]
bantime = 3600
findtime = 600
maxretry = 5

[sshd]
enabled = true
port = 22
logpath = /var/log/auth.log

[nginx-http-auth]
enabled = true
port = http,https
logpath = /var/log/nginx/error.log
```

启动：
```bash
sudo systemctl start fail2ban
sudo systemctl enable fail2ban
```

## 10. 性能优化

### 10.1 数据库优化

```bash
# 定期优化表
mysqlcheck -u datapivot -p --optimize --all-databases

# 分析慢查询
mysqldumpslow /var/log/mysql/slow.log
```

### 10.2 应用优化

- 启用Redis缓存
- 使用连接池
- 配置合适的worker数量
- 启用Gzip压缩

### 10.3 系统优化

```bash
# 调整文件描述符限制
sudo vim /etc/security/limits.conf
```

添加：
```
datapivot soft nofile 65536
datapivot hard nofile 65536
```

## 11. 故障排查

### 11.1 常见问题

#### 服务无法启动
```bash
# 查看服务状态
sudo systemctl status datapivot

# 查看日志
sudo journalctl -u datapivot -n 100

# 检查端口占用
sudo netstat -tlnp | grep 8000
```

#### 数据库连接失败
```bash
# 测试数据库连接
mysql -u datapivot -p -h localhost datapivot

# 检查MySQL状态
sudo systemctl status mysql
```

#### Nginx 502错误
```bash
# 检查后端服务是否运行
sudo systemctl status datapivot

# 检查Nginx错误日志
sudo tail -f /var/log/nginx/error.log
```

### 11.2 性能问题

```bash
# 查看系统负载
htop

# 查看磁盘IO
iotop

# 查看网络连接
netstat -an | grep ESTABLISHED | wc -l

# 查看MySQL进程
mysqladmin -u root -p processlist
```

## 12. 更新部署

### 12.1 代码更新

```bash
# 切换到应用目录
cd /home/datapivot/app

# 拉取最新代码
git pull origin main

# 激活虚拟环境
source venv/bin/activate

# 更新依赖
pip install -r requirements.txt

# 重启服务
sudo systemctl restart datapivot
```

### 12.2 数据库迁移

```bash
# 备份数据库
mysqldump -u datapivot -p datapivot > backup_before_update.sql

# 运行迁移脚本
python scripts/migrate.py

# 重启服务
sudo systemctl restart datapivot
```

### 12.3 前端更新

```bash
cd /home/datapivot/app/frontend

# 拉取代码
git pull

# 安装依赖
npm install

# 构建
npm run build

# 重启Nginx
sudo systemctl reload nginx
```

## 13. 回滚方案

```bash
# 回滚代码
git reset --hard <commit_hash>

# 恢复数据库
mysql -u datapivot -p datapivot < backup_before_update.sql

# 重启服务
sudo systemctl restart datapivot
```

## 14. 检查清单

部署完成后检查：

- [ ] 所有服务正常运行
- [ ] 数据库连接正常
- [ ] 前端页面可访问
- [ ] API接口正常响应
- [ ] SSL证书配置正确
- [ ] 日志正常记录
- [ ] 备份任务配置完成
- [ ] 防火墙规则正确
- [ ] 监控系统运行
- [ ] 文档更新完成
