权限显示"未知"问题已修复
========================

修复内容：
1. 后端：修改 backend/api/v1/case_permissions.py，兼容前后端字段名
2. 前端：修改权限显示映射，将 user 值显示为"读写"

测试步骤：
1. 重启后端服务：cd backend && python main.py
2. 刷新前端页面
3. 测试权限管理功能

预期结果：
- 权限列表显示：管理员、读写、只读
- 不再显示"未知"或"普通用户"

注意事项：
- 当前数据库只有 admin 和 user 两种权限
- 前端选择"读写"或"只读"时，后端都存储为 user
- 显示时统一显示为"读写"

详细文档：
- BUGFIX_权限显示未知.md
- docs/权限问题修复总结.md
